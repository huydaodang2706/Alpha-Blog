<div align="center">
  </br></br>
  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6 jumbotron">
      <%= link_to @category.name, category_path(@category), style: "font-size: 2em; font-weight: bold;" %></br>
      <small><%= pluralize(@category.articles.count, "article") %></small></br>
      <% if logged_in? && current_user.admin? %>
        <%= link_to "Edit category", edit_category_path(@category), class: "btn btn-xs btn-primary" %>
      <% end %>
    </div>
    <div class="col-md-3"></div>
  </div>
</div>
